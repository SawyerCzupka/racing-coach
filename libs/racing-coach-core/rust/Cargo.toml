[package]
name = "racing_coach_core"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "_rs"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module"] }
