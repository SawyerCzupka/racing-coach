# https://moonrepo.dev/docs/config/workspace
$schema: './cache/schemas/workspace.json'

# extends: './shared/workspace.yml'

# projects are only apps/libs that include a moon.yml file.
projects:
  - 'apps/*/moon.yml'
  - 'libs/*/moon.yml'
vcs:
  manager: 'git'
  defaultBranch: 'main'
